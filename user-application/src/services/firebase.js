// Central re-export of Firebase Web SDK configuration
export {
  app,
  auth,
  firestore,
  db,
  storage,
  rtdb,
  analytics,
  GoogleAuthProvider,
  EmailAuthProvider
} from "../config/firebase";

export { default } from "../config/firebase";
